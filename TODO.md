# M4 EOD Retrospection + Rule-Based Evolution — Task List

> This file is auto-generated by the Lead Orchestrator from `pipeline/tasks/`.
> It is a read-only mirror for agents and developers.
> **Do not edit this file directly.** The source of truth is `pipeline/tasks/T-XX.json`.

---

## Already Complete

- [x] **T-33** — Regime tagging engine (`src/trading/regime-tagging.ts`)
- [x] **T-39** — Comparison integrity check (`src/signals/personality-filter.ts`)

---

## Wave 1 — Foundation (no dependencies, run in parallel)

- [ ] **T-42** — Migration 010: retrospection + evolution schema columns and schema.ts updates
  - Creates `src/db/migrations/010_retrospection_evolution.sql`
  - Modifies `src/db/schema.ts`
  - Adds `params`, `display_name`, `group_type`, `last_evolved_at`, `evolution_consecutive_applications` to `personality_configs`
  - Adds `sharpe`, `max_drawdown_pct`, `proposed_adjustments_at` to `retrospection_results`
  - Fully idempotent (ADD COLUMN IF NOT EXISTS throughout)

---

## Wave 2 — Retrospection compute modules (depend on T-42, run in parallel)

- [ ] **T-35** — Daily P&L metrics and Beat-Clockwork delta
  - Creates `src/retrospection/daily-metrics.ts`
  - Exports `computeDailyMetrics` and `computeBeatClockworkDelta`
  - Guards against pg NUMERIC-as-string gotcha and division by zero

- [ ] **T-37** — Brier score calibration for signal-driven trades
  - Creates `src/retrospection/brier-score.ts`
  - Exports `computeBrierScore`
  - NULL-safe: skips fixed_time personalities and empty result sets

- [ ] **T-38** — Management effectiveness score computation
  - Creates `src/retrospection/management-effectiveness.ts`
  - Exports `computeManagementEffectiveness`
  - Weighted average by |pnl_pct|; NULL when no closed trades

---

## Wave 3 — Evolution engine (depends on T-33, T-39, T-42)

- [ ] **T-40** — Rule-based evolution engine with integrity cap
  - Creates `src/retrospection/evolution-engine.ts`
  - Exports `runEvolutionEngine` and `EvolutionResult`
  - Implements key user decision: cap adjustment to maintain ≤8pp spread (never block)
  - 7-day cooldown, FROZEN_VIOLATION guard, EVOLUTION_REQUIRE_APPROVAL fail-safe
  - Uses SELECT FOR UPDATE on all comparison-group rows (TOCTOU prevention)

---

## Wave 4 — Job orchestrator (depends on T-35, T-37, T-38, T-40, T-42)

- [ ] **T-34** — BullMQ EOD retrospection job orchestrator
  - Creates `src/jobs/eod-retrospection-job.ts`
  - Exports `createEodRetrospectionQueue` and `createEodRetrospectionWorker`
  - Cron: `0 16 * * 1-5` Asia/Kolkata; holiday check via event_calendar
  - One transaction per personality; ON CONFLICT DO NOTHING for idempotency
  - Per-personality error isolation: one failure does not abort the batch

---

## Wave 5 — API + server wiring (depends on T-34, T-35, T-37, T-38, T-40, T-42)

- [ ] **T-41** — REST API endpoints for retrospection + evolution, and server wiring
  - Creates `src/api/routes/retrospection.ts`
  - Modifies `src/server/index.ts`
  - `GET /api/retrospection` — filtered query with parameterized SQL
  - `POST /api/retrospection/trigger` — enqueue one-off job by trade_date
  - `GET /api/retrospection/evolution/pending` — list unapplied proposals
  - `POST /api/retrospection/evolution/apply/:personalityId` — atomic approval flow
  - Worker started on server startup (gated by SIMULATE / EOD_WORKER_ENABLED)

---

## Dependency Graph

```
T-42 (migration)
 ├── T-35 (daily metrics)      ─┐
 ├── T-37 (brier score)        ─┤─→ T-34 (job orchestrator) ─→ T-41 (API + wiring)
 ├── T-38 (mgmt effectiveness) ─┘
 └── T-40 (evolution engine)* ─┘

* T-40 also depends on T-33 (regime tagging, DONE) and T-39 (integrity check, DONE)
```

---

_Generated from `pipeline/tasks/` — do not edit manually._
