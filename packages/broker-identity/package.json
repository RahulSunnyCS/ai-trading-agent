{
  "name": "@trading/broker-identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical broker identifiers and the shared RFC 6238 TOTP generator used by every broker login path",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": {
    "@trading/notify": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
