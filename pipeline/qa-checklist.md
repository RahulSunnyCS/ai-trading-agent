# QA Checklist — Milestones 0 / 0.5 / 1

Generated by QA Planner · Phase 1 · Risk: MEDIUM · Lane: feature-full

---

## 🔴 Critical — All must pass at Automation Gate for Gate 2

| ID | Area | Test Case | How to Test |
|---|---|---|---|
| C-01 | P&L Sign | Short straddle: entry_price=100, exit_price=130 → pnl = -30 per unit (loss when straddle rises) | Property test (fast-check) in T-61 |
| C-02 | P&L Sign | Short straddle: entry_price=100, exit_price=70 → pnl = +30 per unit (profit when straddle falls) | Property test (fast-check) in T-61 |
| C-03 | SL Trigger | Hard SL fires at exactly entry*(1+sl_pct); does NOT fire at entry*(1+sl_pct-ε) | Unit test T-16 |
| C-04 | TSL Trigger | Trailing SL ratchets down on favorable moves; fires at min_observed*(1+tsl_pct) | Unit test T-16 |
| C-05 | EOD Exit | EOD square-off fires at EOD_EXIT_TIME in IST; does NOT fire 1s before | Unit test T-16 with FixedClock at boundary |
| C-06 | Daily Loss Cap | Daily loss cap halts new entries when sum(net_pnl for today) < -MAX_DAILY_LOSS | Unit test T-15+T-16 |
| C-07 | Migration | bun run migrate applies all migrations idempotently (second run = same DB state) | Integration test T-63 |
| C-08 | Migration | Migration 001 creates hypertables, straddle_1min CAGG, and compression policy; canary query validates all three | Integration test T-63 |
| C-09 | Migration | TimescaleDB extension absent → migration fails with human-readable error, does not corrupt schema_migrations | Integration test T-63 (vanilla PostgreSQL) |
| C-10 | ATM Strike | getAtmStrike('NIFTY', 22334) = 22350 (round to 50pt) | Unit test T-11 |
| C-11 | ATM Strike | getAtmStrike('BANKNIFTY', 48750) = 48800 (round to 100pt, not 50pt) | Unit test T-11 |
| C-12 | ATM Strike | getAtmStrike('SENSEX', 79123) = 79100 (round to 100pt) | Unit test T-11 |
| C-13 | Decimal | P&L accumulation of 100 × ₹0.10 increments equals exactly ₹10.00 (no float drift) | Property test T-61 with decimal.js oracle |
| C-14 | Clockwork frozen | is_frozen=TRUE personality: any evolution rule targeting it throws FROZEN_VIOLATION (not silently skips) | Unit test (T-06 seed + guard check) |
| C-15 | Simulator | SIMULATE=true produces ≥1 paper_trades row after VirtualClock advances past entry time | Integration smoke T-21 |
| C-16 | Feed stall | T-18 watchdog: when no straddle snapshot arrives for >stale_threshold, EOD exit still fires at correct IST time | Unit test T-18 with FixedClock |

---

## 🟡 Functional — Failures cause CONDITIONAL PASS at Gate 2

| ID | Area | Test Case | How to Test |
|---|---|---|---|
| F-01 | Timezone | Entry window blocked before 9:15 AM IST | Unit test T-15 with FixedClock at 09:14:59 IST |
| F-02 | Timezone | Entry window opens at 9:15 AM IST across UTC midnight boundary | Unit test T-15 with FixedClock at 03:44:59 UTC (= 09:14:59 IST) and 03:45:00 UTC |
| F-03 | Timezone | today() returns correct IST trading date at 23:50–00:10 UTC boundary | Unit test Clock.today() with FixedClock straddling midnight UTC |
| F-04 | Fyers Adapter | Adapter connects under Bun runtime (not just Node) | Manual smoke / integration T-09 |
| F-05 | Reconnect | Transient disconnect: adapter reconnects indefinitely with backoff; process stays alive | Unit test T-09 with mock WS that disconnects |
| F-06 | Reconnect | Auth failure (token expired response): adapter stops reconnecting, logs actionable message | Unit test T-09 with mock auth-rejection response |
| F-07 | Redis | streamPublish → streamRead round-trip delivers message to consumer group | Integration test T-63/T-04 |
| F-08 | Redis | On restart with pending messages (XPENDING), position monitor recovers and ACKs without double-exit | Integration test T-18/T-04 |
| F-09 | Straddle Calc | 15s snapshots written to straddle_snapshots with explicit time value (not DB default now()) | Integration test T-13 with FixedClock |
| F-10 | Event-day gate | Entry blocked on a date in BLOCKED_DATES | Unit test T-15 |
| F-11 | Entry window | No entry fires at or after ENTRY_CUTOFF_TIME | Unit test T-15 |
| F-12 | REST API | GET /paper-trades returns 200 with valid JSON schema | HTTP integration test T-19 |
| F-13 | REST API | GET /dashboard/live returns current straddle snapshot | HTTP integration test T-19 |
| F-14 | WebSocket | /ws/ticks broadcasts straddle update to connected client | Integration test T-19 |
| F-15 | Dashboard | React dashboard renders without JS console errors in dev mode | Manual / T-20 dev server |
| F-16 | Dashboard | Live chart updates when new tick arrives over WS | Manual T-20 |
| F-17 | CI | tsc --noEmit passes with zero errors on main branch | T-59 CI pipeline |
| F-18 | Pre-commit | Lefthook blocks commit when .env file is staged | Manual / T-60 |
| F-19 | Pre-commit | Lefthook blocks commit when .env.example contains a credential-shaped value | Manual / T-60 |
| F-20 | Lockfile | bun install --frozen-lockfile succeeds from clean clone (no bun.lockb generated) | CI step T-59 / T-01 |

---

## 🟢 Non-blocker — Logged only, no gate impact

| ID | Area | Test Case |
|---|---|---|
| N-01 | TypeScript | No `any` types outside fyers-api-v3 shim |
| N-02 | Biome | Zero lint warnings on full codebase |
| N-03 | Docker | docker compose ps shows both services (healthy) within 60s |
| N-04 | Build | vite build completes without errors |
| N-05 | Coverage | Vitest HTML coverage report generated (no threshold gate) |
| N-06 | Integration | TimescaleDB CAGG refresh produces correct 1-min OHLC from synthetic ticks |
| N-07 | Simulator | SIM_TICK_INTERVAL_MS=100 produces ticks at correct interval |
| N-08 | VIX | VIX captured as nullable from broker tick; null does not crash entry engine |
| N-09 | Angel One | Angel One stub returns NotImplemented error cleanly (does not panic) |
| N-10 | Profit target | Profit target exit fires and sets exit_reason=TARGET correctly |
| N-11 | Date.now() ban | Biome lint catches any Date.now() / new Date() outside clock.ts |
