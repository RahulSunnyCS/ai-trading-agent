{
  "risk_level": "LOW",
  "triggers": [
    "Read-only frontend wiring of existing dashboard tabs",
    "No backend files touched (user constraint: strictly frontend-only)",
    "No authentication, no sessions (single-instance tool — auth NOT APPLICABLE per business.md)",
    "No PII stored or processed by this change",
    "Does not touch payment/billing code (Pricing tab left unchanged)",
    "No new public-facing API surface — consumes endpoints the running server already serves"
  ],
  "mandatory_agents": ["architecture-reviewer"],
  "tags": ["frontend"],
  "lane": "feature-fast",
  "sprint_count": 1,
  "human_gates": 3,
  "notes": "User constraint locked at Gate-0: strictly frontend-only, zero backend edits. FE wires the three placeholder tabs (Live, Trades, P&L) to endpoints the RUNNING server (src/server/index.ts) actually serves: GET /api/trades (real DB), WS /ws/ticks (synthetic ticks, shape {type,symbol,ltp,timestamp}), GET /api/straddle/latest (stub null), GET /api/positions (stub []). Build only in canonical src/frontend/. Stale top-level frontend/ tree and orphaned src/api server left untouched (flagged as recommendation, not removed)."
}
